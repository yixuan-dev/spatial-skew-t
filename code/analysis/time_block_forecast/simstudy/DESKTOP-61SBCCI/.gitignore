# Generated by the driver at run time, not tooling:
#   calls_*.txt  the missing-cell call list for one batch
#   *.log        Start-Transcript output
# The transcript on the machine that actually runs the fits is worth
# keeping as provenance -- add it explicitly with `git add -f` there.
calls_*.txt
*.log
